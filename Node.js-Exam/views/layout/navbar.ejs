<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Taskly - Minimalist Task Management</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="logo">Taskly</a>
            <ul class="nav-links">
                <% if (user) { %>
                    <li><a href="/tasks">My Tasks</a></li>
                    <% if (user.role === 'admin') { %>
                        <li><a href="/tasks">All User Tasks</a></li>
                    <% } %>
                    <li><a href="/tasks/add" class="btn-primary">Add Task</a></li>
                    <li><a href="/auth/logout" class="logout-link">Logout</a></li>
                <% } else { %>
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/register">Register</a></li>
                <% } %>
            </ul>
        </div>
    </nav>
